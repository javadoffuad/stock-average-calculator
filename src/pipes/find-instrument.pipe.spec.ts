import { FindInstrumentPipe } from './find-instrument.pipe';

describe('FindInstrumentPipe', () => {
  it('create an instance', () => {
    const pipe = new FindInstrumentPipe();
    expect(pipe).toBeTruthy();
  });
});
